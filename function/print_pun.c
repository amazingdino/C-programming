#include <stdio.h>

void print_pun(void)
{
    printf ("To C, or not to C: that is the question.\n");
}
// same as the countdown.c where you just stored the printf statement into "print_pun" function.
int main(void)
{
    print_pun();
    return 0;
}