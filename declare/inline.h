#ifndef AVERAGE H
#define AVERAGE H
inline double average (double a, double b)
{
return (a + b) / 2;
} // don't really understand how this goes to immediately to a + b / 2 if you
// declare as x = (a + b) => x (a+b) /2 // like is it to shorten it?
#endif