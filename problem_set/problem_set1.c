#include <stdio.h>

int string(char str[])
{
    if 
}

int main(void)
{
    char ch[27];
    printf("Enter characters: ");
    scanf("%s", ch);

    printf("The length of the string is %d\n", string(ch));

    return 0;
}