/*
*
*
*
*/

#include <stdio.h>

int main (void)
{
    
    int a, b;

    scanf ("%d %d", &a, &b);
    if (a > b){
        printf("a is bigger than b");
    }
    else {
        printf("a is smaller than b");
    }
    return 0;
}