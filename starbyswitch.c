#include <stdio.h>

int main(void)
{
    int i, j;

    for (j = 1; j <= i; j++) 
    {
    switch(1) 
    {   // expression is 1 (always true for case 1)
        case 1:
            printf("*");
            break;
    }
}

return 0;
}